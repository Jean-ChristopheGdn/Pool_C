#include <unistd.h>

void my_putnbr(int n)
{
  int i=0;
  char str[i] = {n};
  while ( i != '\0')
    {
      write (1, str[i], 1);
    }
  
}

int main (int argc, char *argv[])
{
  my_putnbr(42);
  my_putnbr(-42);
  return (0);
}
