#include <unistd.h>

void my_true_loop(unsigned int n)
{
  int i = 0;
  while (i < n )
    {
     write (1, "+", 1);
     i++;
    }
  write (1, "\n", 1);
   
}

int main()
{
  my_true_loop(5);
  return (0);
}
